# CP10 — F3 Studio Audio on the capability coordinator + Suno (LIVE)

**Original CP10 baseline: F3 released and verified LIVE.** This section records the original release; the continuation audit below records the subsequently found gaps and their current status.
Baseline `6768b56` → delivered on `main` `9387168`; deployed to https://xsuper.dev the same day.

## Delivered

Backend
- Migration `2026_09_22_100006_add_capability_to_audio_jobs`: the same coordinator columns image/video carry (`capability_revision_id`, `routing_identity`, `price_tokens`, `dedup_key`, `payload_fingerprint`, `reference_asset_ids`) plus `settings` for declared provider flags the fixed `voice/speed/duration/tempo` columns cannot express. All nullable; legacy rows untouched.
- `MediaGenerationCoordinator::startAudio` — mirrors `startImage`: resolve → validate → price/hash 409 guards → dedup/idempotency → reserve → persist (revision + routing + price) → dispatch. Tempo remains prompt-level guidance ("Tempo guidance: approximately N BPM.") exactly like the legacy pipeline, enforced against the model's character limit.
- `KinoviProtocol`: curated **Suno prompt-only audio** — `suno-music` (16 credits/request upstream; `instrumental` + `custom` lyrics flags; Kinovi returns two tracks) and `suno-sounds` (4 credits; plain prompt). `suno-remix`/`suno-sample` stay excluded: they need uploaded input audio. `audioTask()` builds the createTask body.
- `AiProviderTransport`: `submitAudio`/`audioStatus` gained kinovi branches (`createTask`/`recordInfo`), reusing `kinoviTaskState`.
- `AudioGenerationService`: shared pipeline accepts kinovi providers; music submit omits `duration` when the model has none and forwards `settings` flags.
- `AudioController`: **the emergency pause now covers both audio paths** (it previously covered neither); capability submissions (`operation`) route via the coordinator; while activation is restricted a non-coordinator member keeps the verified fal path and **never sees kinovi audio** (no dead controls). `MediaModelConfig::allowedFor` dropped its stale audio→fal-only guard (the curated kinovi whitelist already constrains).
- Capability contract: music declares `duration` only when the model has it, `tempo` guidance, and Suno's `instrumental`/`custom`; `publicModel` exposes the flags.

Frontend (`AudioGenerator.jsx`)
- Submits the capability payload (`operation: text_to_speech|music`); params travel only when the model declares them, so Suno sends nothing it does not support.
- Suno toggles: "Gunakan lirik sendiri" (prompt becomes lyrics; label/placeholder switch) and "Instrumental tanpa vokal". Duration slider hides for models without one; validation no longer demands a duration the model lacks.

## Verification

- Isolated suite **483 passed** (adds `AudioCoordinatorRouteTest`: coordinator routing, Suno flags/tempo reaching the provider body, unknown-param rejection with zero charge, kill switch on both paths, non-coordinator gating). FE unit 7; build clean.
- **LIVE on prod (user tes, kemudian dihapus):**
  - `suno-sounds` → completed, `cap_rev=5`, hasil 454 KiB.
  - `suno-music` (instrumental) → completed, `cap_rev=6`, hasil 4,8 MiB; `settings={"instrumental":true,"custom":false}`.
  - `fal-ai/kokoro` speech → completed, `cap_rev=7` (fal path through the same coordinator).
  - Studio UI: Suno hides duration, shows the two toggles; player renders with seek + `/api/audio/{job}/asset` download; Library shows Audio 3.
- Billing observed: 25 + 100 + 20 tokens reserved→settled once each; QA artifacts and account removed after verification.

## Pricing (owner decision, adjustable in Admin)

| Model | Upstream | Set to |
|---|---|---|
| `suno-music` | 16 Kinovi credits ≈ $0.073/request (2 tracks) | **100 tokens** |
| `suno-sounds` | 4 credits ≈ $0.018 | **25 tokens** |

Anchor: existing audio models are 20 tokens; kinovi credit ≈ $0.00455 (derived from published avatar $/s vs credits/s).

## Kinovi inventory (brief §8 "sisa 13") — resolved from the published index + API probes

Live and integrated: 8 image, 6 video (t2v), 2 audio. Published but NOT yet integrated: `minimax-h3-turbo-avatar-talking` (talking avatar; F4 target), `minimax-h3-turbo-image-to-video`, `minimax-h3-turbo-i2v`, `wan3.0-image-to-video`, `wan3.0-prime-image-to-video`, `wan3.0-ref-to-video`, `wan3.0-prime-ref-to-video`, `seedance2-5-edit`, `seedance2-5-extend`, `happyhorse1.0` (i2v-only), `suno-remix`, `suno-sample` (input audio needed). Documented-but-broken upstream: `minimax-h3-turbo-text-to-video` answers "Unknown model" at createTask despite a live model page.

## Original CP10 limitations

- At this baseline, Suno music returned **two tracks per request but only the first was stored** as the job result. The continuation below preserves both.
- At this baseline, the audio/video studios did not send `expected_price_tokens`/`expected_capability_hash`. The continuation below adds these guards and unresolved-request recovery.
- `suno-sounds` has native `type/bpm/key` fields; the studio sends tempo as prompt guidance instead of native `bpm` (parity with every other music model).

## Continuation audit — F4/F5/F6b production release

Feature commit `d78d61bb5f546d83301e37d60a7ace6059a8f290` was fast-forwarded into `main`, pushed and deployed to https://xsuper.dev. Migrations `100007`–`100011` ran as production batch 10 after maintenance, empty-queue guards and a validated database backup. Initial backup: `/root/xsuper-backups/media-f4-f6-20260922-175918/` (database dump: 627,004 bytes). Follow-up `0f5b2c139ccb2c4aa25481b77c62c93ba91c1266` deployed the catalog-limit and native-download fixes with no dependency/migration changes; backup `/root/xsuper-backups/media-f4-f6-20260922-194225/` includes a 2,958,222-byte database dump. Both deployments returned public health HTTP 200 with all four application services active.

Owner authorization (23 September 2026): merge into `main`, push and deploy with backup/migration safeguards; enable new talking-avatar at **10 tokens × selected integer seconds** and Trellis 2 at **350 tokens per result**. Preserve existing sale prices. Total paid live verification is capped at **US$1**; never resubmit a generation whose provider acceptance remains uncertain.

### Implemented and locally exercised

- **F3 gaps:** canonical indexed audio outputs preserve both Suno tracks under one charge; player switching, seek, original downloads, Library and quota accounting consume the same output list. Existing production history contains no retained Suno job requiring backfill.
- **Admission/retries:** image/video/audio/avatar send quote/hash guards and retain the exact unresolved request body and idempotency key. Replay uses the job's immutable capability revision and original fingerprint format, before current price/quota/asset-expiry checks. A browser scenario hid an accepted response, returned 429 on retry, changed the live model price, then recovered the same job with one charge; cancellation released that reservation.
- **F4:** owned portrait + speech upload/selection, generated-speech reuse, explicit face/voice consent, per-second pricing and shared video lifecycle/history/cancellation. The follow-up below replaces Kinovi's failed pull-URL delivery with its official upload API and adds Fal photo/audio execution. Kinovi's 480p wire value is `"0.2"`, not `"480p"`. Billing uses the selected validated integer duration × model token price; it does **not** round up the uploaded audio's actual duration.
- **F5:** `fal-ai/trellis-2`, image-to-3D, fixed 512 resolution/1024 texture/50,000 decimation target; optional integer seed. Private original GLB storage, bounded validation, authenticated download and a lazy viewer with orbit/zoom/pan/reset. Official authenticated metadata/pricing probes returned 200; `$0.05/UNIT` means five units ($0.25) for this 512 configuration, not $0.05 per generation. A real production inference completed; evidence below.
- **F6b:** bounded schema-backed fal discovery (API limit 1–10; admin page choices 5/10), preserved curation, explicit imported/tested/reviewed/published states, unsupported-schema blockers, revision comparison/review, rollback and disablement. Provider summary and scoped model queries replace loading the entire admin catalog. Local browser proof published r1 then r2, restored r1 and disabled it; an unsupported required object kept publication disabled. These lifecycle fixtures did not contact a provider.
- A deterministic discovery smoke check imported a 3D candidate against the already curated Trellis model. Sale price/activation/configuration, capability quote and executor compatibility remained unchanged; the unsupported generic candidate did not disable the dedicated 3D executor. The transaction was rolled back.
- **UI corrections:** required fields do not show premature errors on first render; 390px avatar/3D/admin views have no document-level horizontal overflow. Admin pricing now distinguishes tokens/second from tokens/job and tokens/result. Resource CSP retains the strict private-GLB policy while permitting in-memory `blob:`/`data:` texture reads on the application page.
- The full browser suite reproduced a separate mobile Deposit defect: equal-width tabs overflowed with the longer Indonesian storage label. Tabs now retain their content width within a bounded horizontal scroller, and keyboard navigation reveals the whole focused tab. Actual 320px/390px and desktop inspection plus the existing deposit E2E cover the fix.

### Verification evidence and release gates

- At the `0f5b2c1` release: full PHP application suite **531 passed, 3,809 assertions**. Frontend unit suite: **8 passed**. ESLint and production build pass; the existing large-entry warning remains, and the approximately 1.03 MB model-viewer bundle is lazy-loaded. Later native Avatar verification is recorded separately below.
- Actual browser: two decoded audio fixture tracks (3s/5s), play/pause/seek/switch, avatar input/consent/cancel/replay, admin revision transitions and mobile layout. These prove local UI/lifecycle behavior, not paid provider success.
- The official textured Trellis GLB rendered after fixing a reproduced CSP texture-fetch failure. Viewer controls changed/reset camera state. A real browser download preserved **19,508,884 bytes**, SHA-256 `a8b528d7715ada8dfa5c7dd0ac63abe47c682752db86bfbf4625f516cc443b2a`.
- Full browser suite: **31 passed** (4.6 minutes). Restored current provider/detail/pricing/API-key navigation, verified member fixtures, one-time API secrets and explicit external-API permission. The recovery component fixture loads as a same-origin module under the real CSP, not an inline script. Obsolete wallet-top-up/default-count assertions were removed; refund, key rotation/revocation, deposit credit isolation and subscription approval remain exercised.
- Migrations `100007`–`100011` have run on local PostgreSQL and production. They preserve indexed audio, add 3D/catalog state, permit avatar video mode and normalize historical `int`/`bool` capability types without changing historical fingerprints. The audio/type migrations are not safely undone with a blind rollback.
- Local continuation fixtures have been removed: QA user 8, providers 4/5, their model/job/asset records, the exact cancelled queue row and canonical private files (zero bytes remaining at the collected paths). Temporary credentials/downloads/smoke scripts were removed. The original local media worker was restored and processed a harmless readiness probe; managed verification tabs were closed.
- All live release gates are closed: the native Avatar follow-up is deployed, both Avatar providers completed generation/playback/original downloads, the final ledger is reconciled, and evidence-bound QA fixtures are removed. Suno/GLB downloads, viewer controls and Library/quota checks passed. The earlier Avatar HTTP 403 failures remain recorded below; official uploads resolved the Kinovi delivery path without weakening edge controls. A final comparison found zero changes to all 792 pre-existing model identities, prices and curation fields.

### Corrected Kinovi inventory

The [published index](https://kinovi.ai/llms.txt) contains **31 IDs**, not a reliable “remaining 13” subset. The working adapter contains **17 curated IDs**: 8 image, 6 prompt-video, 2 prompt-audio and 1 new talking-avatar. “Curated” describes implemented request contracts; it does not claim every ID was generated successfully in this session.

| Curated category | Exact IDs |
|---|---|
| Image (8) | `gpt-image-2`, `gpt-image-2.5-sunburst`, `gpt-image-2.5-flare`, `seedream-5.0-pro`, `nanobanana-pro`, `nanobanana2`, `midjourney-v8`, `midjourney-v7-niji` |
| Prompt-video (6) | `seedance2-5`, `seedance-20`, `seedance2-fast`, `seedance2.0-mini`, `wan3.0-text-to-video`, `wan3.0-prime-text-to-video` |
| Prompt-audio (2) | `suno-music`, `suno-sounds` |
| Talking-avatar (1) | `minimax-h3-turbo-avatar-talking` — successful live generation/playback/original download through official reference uploads; 60 tokens settled once for six selected seconds. Two earlier pull-URL failures each released 60 tokens; historical evidence below. |

The **14 remaining IDs** below all have official pages marked live. Documentation is not account/execution proof. `needs_handling` here is the audit disposition, not a claim that these rows were imported/published in production.

| Exact ID / official source | Disposition and concrete blocker |
|---|---|
| [`minimax-h3-turbo-image-to-video`](https://kinovi.ai/models/minimax-h3-turbo-image-to-video) | `needs_handling`: prompt required; references optional in its schema, not proven image-required. Native resolution is `"0.2"`/`"0.41"`; ordered image/video/audio arrays and contradictory limits need explicit handling. |
| [`minimax-h3-turbo-i2v`](https://kinovi.ai/models/minimax-h3-turbo-i2v) | `needs_handling`: optional 0–9 image/0–3 audio references, no video references; 480p/768p display values, only 16:9/9:16. Not the other MiniMax endpoint's megapixel contract. |
| [`wan3.0-image-to-video`](https://kinovi.ai/models/wan3-image-to-video) | `needs_handling`: required `imageUrls` (1–2), ordered first/optional-last frame; prompt may be omitted when media exists. |
| [`wan3.0-prime-image-to-video`](https://kinovi.ai/models/wan3-prime-image-to-video) | `needs_handling`: independently documented required first/optional-last frame; Prime routing/pricing cannot be inferred from the text-to-video ID. |
| [`wan3.0-ref-to-video`](https://kinovi.ai/models/wan3-ref-to-video) | `needs_handling`: optional ordered images/video/audio, mutually exclusive document/link inputs, source-dependent duration limits; minimum-valid-input rule is incomplete. Do not invent mandatory images. |
| [`wan3.0-prime-ref-to-video`](https://kinovi.ai/models/wan3-prime-ref-to-video) | `needs_handling`: distinct Prime route; same substantive multimodal/exclusivity/duration handling, no account proof. |
| [`seedance2-5-edit`](https://kinovi.ai/models/seedance2-5-edit) | `needs_handling`: source video required, output follows source duration, do not send `duration`; schema names `seedance2-5-video-edit` instead of the page ID. Alias acceptance is unverified. |
| [`seedance2-5-extend`](https://kinovi.ai/models/seedance2-5-extend) | `needs_handling`: source video plus 4–30 added seconds; no aspect-ratio override; reference-plus-extension billing. Schema instead names `seedance2-5-video-extend`; unresolved ID/output-contract conflicts. |
| [`happyhorse1.0`](https://kinovi.ai/models/happyhorse) | `needs_handling`: documented `happyhorseMode` t2v/i2v/r2v/video-edit and `resolution` key. Earlier account probe required a first frame; current t2v-default documentation conflicts. The earlier “i2v-only” shorthand is not a complete current contract. |
| [`suno-remix`](https://kinovi.ai/models/suno-remix) | `needs_handling`: source `musicId` or `audioUrl`, prompt, custom/keepLyrics handling; two output tracks. Visible formal schema is incomplete. |
| [`suno-sample`](https://kinovi.ai/models/suno-sample) | `needs_handling`: source ID/URL plus sample-window endpoints; prompt is an optional label. Returned sample metadata lacks a concrete schema and conflicts with generic two-track copy. |
| [`minimax-h3-turbo-high-dynamic`](https://kinovi.ai/models/minimax-h3-turbo-high-dynamic) | `needs_handling`: required prompt + 1–9 images; `"0.2"`/`"0.41"` megapixel values. An enabled profile does not supply the missing adapter. |
| [`minimax-h3-turbo-cinematic`](https://kinovi.ai/models/minimax-h3-turbo-cinematic) | `needs_handling`: required prompt + 1–9 images; different `"0.41"`/`"0.58"` values for 480p/768p. Do not reuse High Dynamic's mapping. |
| [`minimax-h3-turbo-text-to-video`](https://kinovi.ai/models/minimax-h3-turbo-text-to-video) | Account availability unresolved: earlier authenticated `createTask` returned `Unknown model`, despite prompt-only/live documentation. Keep excluded; this is not a reference-required endpoint. |

High Dynamic and Cinematic were omitted from the earlier remainder list. Existing enabled production profiles are still excluded from execution by the curated allowlist; this audit has not changed their activation or prices.

### Production evidence — continuation

- **3D:** job `2a09949b-0d1a-4a47-a1c6-64ed1fccf8e9` completed with **350 tokens settled once**. The production browser rendered the colored cube; orbit, zoom, pan and reset changed/restored camera state. Its native original download preserved **1,509,896 bytes**, SHA-256 `58bb94c04376e45404fc7a8fd876f09fe857c1d38457edb3cbde660ed8f91487`. This is distinct from the larger official local fixture.
- **Suno:** job `ceec6fc4-0b51-4b33-ab60-4e00b9a52b47` completed with **100 tokens settled once**, preserving **both** original MP3s: 985,881 bytes / 41.5335 seconds and 849,945 bytes / 37.2135 seconds. Both decoded/played; seek and track switching worked. Native downloads matched private originals: SHA-256 `7c2cee7060736075e28484f5856fb6c81e137c4a81afc3d3fadbe6a87a7f8391` and `177dedd0756dee6281f37c2f846311c2d0537be33fccfc1cc988cf7424f2e8ae`. Library showed two audio entries, eight total files and exactly **6,074,733 bytes** occupied before the next Avatar verification.
- **Operator diagnostic failure:** preceding Suno job `4addd203-b8c1-4e4d-8a6e-d4bfcb3b33fa` failed locally after an isolated diagnostic mistakenly left Laravel's `Http::preventStrayRequests()` factory in the media daemon. Its 100-token reservation was released. The worker was restarted before the distinct successful request above. Null task ID/refund alone was not accepted as cleanup proof: the bounded job-specific attestation and its evidence are retained under the closure record below.
- **Avatar attempt 1:** job `91ac973c-e566-4800-b60a-3fcad8ce476c`, six selected seconds and a 5.7736-second synthetic speech file, received Kinovi `reference_media_inaccessible`: `Reference image 1 fetch failed (HTTP 403). Please replace it.` Its 60-token reservation was refunded once. Kinovi reported 9.36 credits used; application refund does not imply provider refund. Signed grants returned 200 when directly probed, including a worker-issued grant. The provider fetch was not found in the origin access log for that image/time; this does **not** establish Cloudflare as the cause.
- **Fresh reference:** fictional adult portrait `1a71cf27-cb63-4d1f-ba54-a3363ece8d97` completed through `fal-ai/flux/schnell` for 10 tokens. Its inspected frontal face was used for six-second Avatar `5ce46f09-aa40-4b93-88b7-723a9aed9a62`, which failed with the same reference HTTP 403, released 60 tokens and reported another 9.36 Kinovi credits used. This exact image's grant returned 200 and its origin log contained no matching 403. Replacing the image alone did not fix access. The owner then reported a successful direct Kinovi web task (`task_anlljmxsocq17qnqtmhi8oii`); that success is accepted, not re-probed. The chosen fix is official reference uploads, not attributing the failure to Cloudflare or weakening edge controls.
- **Native download defect:** Chromium's download navigation omits headers present on ordinary fetches. The old device fingerprint therefore admitted it as a different device and returned 403 instead of the original file. The fix binds an authenticated browser session to its owner-scoped device row; each request still enforces active/pending/blocked state, and plugin bearer tokens remain stateless. Missing/deleted/foreign session bindings use the original admission logic. Regression coverage includes real asset responses and blocking/pending transitions; no blob workaround or admission bypass was introduced.
- **Spend ceiling:** final conservative booking for three Kinovi Avatar attempts, one Fal Avatar result, one Trellis result, two Suno attempts (including the locally blocked one) and one portrait is approximately **US$0.6794**, below the approved US$1. This includes the regular $0.15 Fal estimate rather than its promotional $0.075 quote. It is a reservation estimate, not an asserted provider invoice.

### Native Avatar follow-up — deployed and live verified

- Owner approved Kinovi's documented temporary upload retention (approximately 24 hours) and requested Fal `minimax/h3-max-turbo/image-to-video` in the same workspace. Newly imported, previously disabled/unpriced Fal profile **820** was activated as native Avatar at **10 tokens/second**, guarded by its exact identity and audited as `continuation.fal_avatar.enabled`. Kinovi Avatar **805** remains 10 tokens/second and Trellis **806** remains 350 tokens/result. All 792 pre-existing model prices and curation fields are unchanged.
- Native integration `d7a26c43ccc032cac2cb38b173ac72987c1d58b9` deployed after backup `/root/xsuper-backups/media-f4-f6-20260922-205502/` (database 2,960,977 bytes). Layout correction `aff6ce17546bc592794cf7bf1e70d6bb4e4d28c5` followed with backup `/root/xsuper-backups/media-f4-f6-20260922-211147/` (database 2,962,522 bytes). Both deployments returned public health HTTP 200, four active services and empty queues. No additional dependency or migration changes were needed.
- [Kinovi upload contract](https://kinovi.ai/openapi.json): authenticated `POST /api/v1/uploads`, streamed unauthenticated PUT to its validated presigned destination, then authenticated confirmation through the configured API. Exact path/URL/size/MIME and expiry checks gate submission. Public-DNS pinning, redirect/proxy restrictions and credential separation remain enforced. Both owned references must finish staging before the paid `createTask`; there is no fallback to the failed XSuper pull URL.
- [Fal model contract](https://fal.ai/models/minimax/h3-max-turbo/image-to-video/llms.txt): required prompt/photo/audio, selected integer duration **5–15 seconds**, fixed **480P**, no unsupported aspect/pro controls. Owned references are inline; audio must be **at least 2 seconds** and **at most 15,000,000 bytes**, checked before reservation. UI explicitly describes soundtrack replacement, **not guaranteed lip-sync**.
- Fal status/result requests use `minimax/h3-max-turbo/requests/{id}`, matching the official [queue SDK](https://github.com/fal-ai/fal-js/blob/main/libs/client/src/queue.ts) owner/alias routing, not the nested inference subpath shown by generated OpenAPI.
- Full PHP suite after admission fixes: **578 passed / 4,145 assertions**. After the canonical polling-path correction, the final focused native suite passed **59 tests / 428 assertions**, including submission, polling, private result download and single settlement. Frontend unit **8 passed**, ESLint/build passed; existing E2E **31 passed** before the final audio-limit copy and CSS correction. The CSS correction built successfully and was verified on the actual production surface below; no post-CSS full E2E run is claimed.
- Real production-host FFprobe checks reject one-second WAV/MP3/M4A/WebM and accept two-second clips, including codec priming. A 30-second non-fast-start M4A is readable through a seek cache. Inspection decodes only a bounded initial interval, allows only cache/pipe protocols, and has five-second/64-KiB-output limits; the configured binary is executable by `www-data`.
- **Kinovi success:** job `3555812f-2328-4711-96bc-e4747dd7e074` completed after both official reference uploads and confirmations. **60 tokens settled once** for six selected seconds. Native download: **738,205 bytes**, SHA-256 `8c981e7af0095951a0a3a107503e8d900a010d1df980b7809dc857fc73dae740`. Browser playback decoded video/audio without error: **448×448**, **6.625 seconds** including the provider's tail.
- **Fal success:** job `23207aef-d646-453c-8fee-4d8078b664ee` completed through canonical root polling. **60 tokens settled once** for six selected seconds. Native download: **1,483,291 bytes**, SHA-256 `a362846eb4346210d11625940773de8b061ae7671012d197322c3a83a5272cec`. Actual post-deployment playback decoded video/audio without error: **480×480**, **6.592 seconds**. Supplied-speech waveform correlation was approximately 0.960 for Kinovi and 0.997 for Fal; this checks audio preservation, not lip-sync quality.
- **Real output layout:** square generated video exposed an intrinsic grid minimum: a 510px video overlapped a 450.56px monitor and intercepted download clicks. `.studio-video-screen video { min-height: 0; }` fixes the sizing constraint rather than bypassing download UI. Fresh production CSS, with no injected inline override, passed frame/control hit-testing at **320, 390, 1440 and 1920px**. Both Avatar originals were downloaded through the browser and matched their private-file hashes.
- **Final Library/ledger before fixture removal:** 10 owned files / **8,296,229 bytes**: two Avatar, two Audio, one Image, one 3D and four references. The temporary 1,000-token grant reconciled to **800 deducted − 220 refunded = 580 spent**, leaving **420**. Successful jobs settled once; failed reservations released once.

### Complete fal discovery report

Run `e784d556bdec31d08cbd0c9fc0aeab40` completed **152 pages / 1,512 unique endpoints / 1,512 new source revisions**, with no remaining cursor or pending page. The first limit-25 request had returned 400 before committing a page; its failed-run evidence was archived, and the completed run used the observed schema-expanded maximum of 10.

- **214 imported candidates**, **1,298 needs_handling**. Discovery does not mean runtime support, live testing or publication.
- At discovery completion, **1,502 new model profiles were disabled and unpriced**. Only newly imported profile **820** was subsequently activated through the separately authorized native Fal Avatar contract above; the remaining 1,501 were not published by this work. All **792 pre-existing model IDs** retained their identity, display/category, provider, sale price and activation; none changed from available to unavailable. Final post-cleanup checks confirmed native profiles **805/806/820** remain enabled, available and executable at **10/350/10** respectively. A generic unsupported Trellis candidate does not disable its curated native executor.
- Per-model, per-status inventory: [`2026-09-23-fal-catalog-inventory.json`](2026-09-23-fal-catalog-inventory.json). It records 1,520 fal profiles, including eight older chat profiles outside the 1,512 observed endpoints, source-schema hashes, operations and blockers. It is a point-in-time discovery report, not a claim that later native verification created no further snapshots. Private source report SHA-256: `d61eb5a4a5d2c9ea61a5c4bf4240e4b8842443fc6147989ddd9ae9b1c8707d4f`.
- Conservative normalization limitations remain explicit: six image candidates are blocked solely by `_fal_ui_field`; six other schemas have unresolved/empty operation contracts. Generic metadata labels classify Kokoro as music although its curated native executor remains TTS. Schema presence does not prove a fully resolved executable contract. These candidates were not silently published or charged.

### Production closure and fixture disposal

- QA member **11** was removed only after the evidence-bound cleanup guard passed. Its sessions, API keys/devices, dependent records and collected application files were removed; final proof reports **zero owned files / zero owned bytes**, no QA user and no retained QA credentials. Existing administrator actors **1 and 4** and the three authorized public native profiles were preserved.
- One narrowly scoped operator attestation covered only failed audio row **5** / job `4addd203-b8c1-4e4d-8a6e-d4bfcb3b33fa`, fixture `63fa3816-f0b0-4fd7-a849-546f857a8fe2`. Evidence binds actor 1, QA 11, the original row/error/timestamp, one debit/refund, zero submission/polls/outputs/leases/queued work, and the same-worker timeline: guard installed 18:47:37Z, failure 18:48:41Z, worker restart 18:53:34Z. The original exception trace was **not captured**; the attestation retains that limitation rather than inventing a trace. Foreign-actor/stale-evidence negative checks were rejected without mutation, and unrelated cleanup blockers remained enforced.
- Cleanup evidence SHA-256: `cad07db19f6e6deb01a9ec939ea626db58336c174bcae0fa7e99f8fbcf7a253e`; bounded attestation SHA-256: `24295e5bd5784be98f39503d0c58b72dc73ec3cb882b68146c93297d2f70863a`. Sanitized browser/audio-probe/curation/cleanup evidence and the cleaned state remain private; the original blocker and accepted exception are retained in the cleanup audit.
- All **10 originals** were size/hash-checked before archival and independently checked again from the off-server archive. Private archive: `/root/xsuper-backups/media-f4-f6-20260922-211147/qa11-originals-cad07db19f6e.tar.gz`, **7,577,326 bytes**, SHA-256 `0ddecaf6cecd2576ab177b77f120913ef5f645e8daad44583963ef686197a857`, root-only permissions. The matching off-server copy is `.dev-local/continuation-originals.tar.gz`. These are intentionally retained proof backups, not live Library/storage-quota files.
- Cleanup also removed **17 remote** release-owned helpers/lock/grant artifacts and **24 local** helper/credential/raw-download/reference files, plus the empty download directory. The worker diagnostic that installed the persistent HTTP guard is removed. Managed production verification tabs are closed. Sanitized evidence, database/media backups and unrelated user work remain intact.
